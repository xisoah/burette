# Create the instance
# If interupted, create latest snapshot
# Create instance in new region with snapshot id
# Remove old snapshot in s3

{ user { order { [ zone, token, instance, price, snapshot-id, key used]

Create instance
Maintain Instance
Wait for CloudWatch
Copy snapshot


Security policy development
AC and identity management

response['Reservations']

D:/Users/sohai/Desktop/burette/env/Scripts/pyuic5.exe -x editor.ui -o edit.py

